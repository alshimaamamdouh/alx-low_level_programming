#include "main.h"

/**
 * _abs - calculate the absolute value
 * @i: the integer that will calculate the absolute
 *
 * Return: the absolute value
 */

int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (-i);
}
